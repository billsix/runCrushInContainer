# Import test conventions

The secret build code is recorded in the imported file referenced below. The string
appears ONLY in that imported file — if the model knows it, the `@`-import worked.

@secret.md
