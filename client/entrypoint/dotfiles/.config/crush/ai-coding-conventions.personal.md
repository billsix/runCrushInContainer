<!--
Personal overlay — @-imported by CLAUDE.md as `@~/.claude/ai-coding-conventions.personal.md`.

This tracked file is intentionally BLANK. It is the default so that a bare
`podman run` (without the Makefile's mount) still resolves the @-import instead of
dangling on a missing file.

Your real personal conventions — identity, your project/repo mapping, your mount
layout, any standing authorizations — go in `~/.ai-coding-conventions.personal.md` on the
host, which `make shell` mounts over this file (auto-creating it empty if absent).
See `ai-coding-conventions.personal.example.md` in this directory for the template, and `FORKING.md` in the
repo root for setup.
-->
