# --- server/ artifacts (never commit) ---
# Python venvs for the Hugging Face CLI / MLX
server/.venv/
server/.venv-mlx/
# downloaded model weights — large
server/models/
*.gguf
# llama.cpp checkout + build
server/llama.cpp/

# --- client/ vendored sources (never commit; large — see tasks/vendor-build-sources-for-airgap-rebuild.md) ---
# Pre-fetched Crush source + Go module deps for an offline (CRUSH_VENDORED=1) rebuild.
client/vendor/

# --- editor / tooling ---
*~
.ruff_cache/

# Crush per-project runtime state (crush.db, sessions) — created when running crush in a dir
.crush/

# image-export artifacts (large; never commit)
client/crushcontainer-*.tar
